# 기획 → 디자인 → 클라이언트 플로우 (표준 작업 시스템)

> **역할**: 새 게임을 만들 때 따르는 **표준 진행 순서**. 프로젝트가 바뀌어도 이 플로우 그대로 재사용한다.
> **핵심 원칙**: ① 콘텐츠 구성을 먼저 정한다(씬은 나중). ② 각 단계 끝에 **사용자 승인 게이트**를 둔다.
> ③ 앞 단계가 확정되기 전에 다음 단계로 넘어가지 않는다.

---

## 단계 요약

```
Stage 0.  컨셉(입력)      Game_Concept.md ─ 이미 컨펌된 간략 컨셉
   │
Stage 1.  게임 구성       [ProjCode]_GameComposition.md ─ 필러 · 콘텐츠 · 루프 (씬 무관)
   │    └── 게이트 A: 콘텐츠 구성·루프·기둥 결정 승인
   │
Stage 2.  콘텐츠별 세부    [ProjCode]_Content_[이름].md ─ 콘텐츠마다 1문서
   │    └── 게이트 B: 콘텐츠 단위 승인
   │
Stage 2b. 화면 설계       [ProjCode]_Screen_[이름].md + 박스 목업(HTML/SVG)
   │    ─ 화면 인벤토리 · 네비 플로우 · 화면별 요소 트리(이름·UI타입·anchor·바인딩)
   │    └── 게이트 B2: 화면 레이아웃·요소 승인  (씬 그룹핑은 아직 안 함)
   │
── 기획 종료 / 디자인 시작 ──
   │
Stage 3.  디자인          ① 화면→씬 그룹핑 ② 프리팹 생성(unityMCP/에디터툴) ③ 리소스(ChatGPT)
   │    └── 게이트 C: 씬 구성·프리팹 승인
   │
Stage 4.  클라이언트       프리팹 바인딩 + 기능 구현 (네이밍 규약으로 요소↔이벤트 연결)
```

---

## 단계별 산출물·책임

| Stage | 산출물 | 위치 | 정하는 것 | 안 정하는 것 |
|-------|--------|------|-----------|-------------|
| 0 컨셉 | `Game_Concept.md` | `Projects/[ProjCode]/` | 장르·한줄컨셉·타깃·레퍼런스 | 세부 전부 |
| 1 게임 구성 | `[ProjCode]_GameComposition.md` | `Projects/[ProjCode]/` | 필러 / 콘텐츠 인벤토리 / 콘텐츠 관계(루프) / 기둥 결정 | **씬 구성**, 콘텐츠 내부 상세 |
| 2 콘텐츠별 세부 | `[ProjCode]_Content_[이름].md` | `Projects/[ProjCode]/` | 콘텐츠별: 목적·화면요소·규칙·데이터·연출·의존 | 화면 레이아웃, 프리팹, 코드 |
| 2b 화면 설계 | `[ProjCode]_Screen_[이름].md` + 박스 목업(HTML/SVG) | `Projects/[ProjCode]/wireframes/` | 화면 목록·네비, 화면별 **요소 트리**(이름·UI타입·anchor·바인딩) | **씬 그룹핑**, 비주얼 디자인, 코드 |
| 3 디자인 | 씬 구성표 + 프리팹(unityMCP) + 리소스(ChatGPT) | 씬/프리팹은 Unity, 문서는 `Projects/[ProjCode]/` | **씬 구성**, 프리팹 생성, 배치, 비주얼 | 기능 로직 |
| 4 클라이언트 | 모듈·스크립트 + 씬 배선 | `Assets/Scripts/` | 기능 구현, 요소↔이벤트 배선, 세이브 | 신규 기획 |

---

## 게이트 규칙 (사용자 승인)

- **게이트 A** — 게임 구성 확정. "콘텐츠 구성·루프가 이대로 맞다"는 승인. 이후 씬/프리팹/코드의 기준선.
- **게이트 B** — 콘텐츠별 세부 확정. 콘텐츠 **하나씩** 승인 → 승인된 콘텐츠만 디자인·구현으로 내려간다.
- **게이트 C** — 디자인 확정. 씬 구성과 프리팹 목록 승인 → 클라 구현 착수.
- 승인 없이 다음 Stage로 진행하지 않는다. 변경은 해당 Stage 문서를 고치고 게이트를 다시 통과.

---

## 표준 양식 (재사용)

| Stage | 빈 양식 |
|-------|---------|
| 1 게임 구성 | [`Game_Composition_Template.md`](Game_Composition_Template.md) |
| 2 콘텐츠별 세부 | [`Content_Spec_Template.md`](Content_Spec_Template.md) |
| 2b 화면 설계 | [`Screen_Spec_Template.md`](Screen_Spec_Template.md) |
| 2b 에셋 매니페스트 | [`Asset_Manifest_Template.md`](Asset_Manifest_Template.md) |

> 새 프로젝트: 리포 복제 → `Game_Concept.md` 추가 → "개발 시작" → Stage 1부터 이 플로우 진행.

---

## 디자인 파이프라인 & 핸드오프 규약 (Stage 2b → 3 → 4)

화면 설계(2b)가 프리팹·구현으로 결정론적으로 이어지도록 **네이밍 규약**을 접착제로 쓴다.

```
화면 설계(2b)                 디자인(3)                        클라이언트(4)
요소 트리                →   프리팹 생성 + 에셋            →   프리팹 바인딩
─────────                    ─────────                        ─────────
요소 이름                →   GameObject 이름(동일)        →   바인딩 키(동일)
UI 타입(~UI/~Panel/~Popup) →  해당 UI 프리팹 타입           →   UIPanel 등 베이스
바인딩 데이터/이벤트       →   (placeholder)                →   IEventBus 구독/갱신
```

- **요소 이름 = GameObject 이름 = 클라 바인딩 키** (세 곳이 동일해야 자동 연결).
- **UI 타입**은 리포 규약 [`06_ui.md`](../.claude/rules/project/06_ui.md)의 `~UI / ~Panel / ~Popup / SystemPopup`를 그대로 태깅.
- **프리팹 생성**: `unityMCP`(mcp-for-unity) 또는 리포 에디터 툴(CreatePrefabs)로 요소 트리 → GameObject 계층. placeholder 텍스처·아틀라스·Addressable 자동(리포 규약 03/05).
- **비주얼 리소스**: 화면 설계의 Image 요소를 모은 **에셋 매니페스트**(`[ProjCode]_AssetManifest.md`)가 **ChatGPT 이미지 생성 입력**이 된다. 파일명=요소명 규약으로 생성 스프라이트를 프리팹 슬롯에 적용.
  - GPT는 출력 크기가 고정(1024²·1024×1536 등)이므로 매니페스트는 **개별 에셋이 아니라 생성 배치(batch) 단위**로 묶는다. 작은 아이콘은 한 장에 그리드로 뽑아 잘라 쓴다.
  - 후처리 필수: `생성 → 배경 제거 → 정수 배율 다운스케일(Nearest) → 색 수 양자화 → 규격 크롭 → 9-slice 지정`.
- **이벤트**: 각 콘텐츠 세부 기획(§6)에 이미 정의된 발행/구독 이벤트를 클라가 요소에 연결.
- **실행 제약**: unityMCP는 Unity 에디터 실행이 필요. **리소스 생성은 사용자가 ChatGPT에서 직접 수행**하므로, 이 리포의 책임은 **매니페스트(프롬프트·규격·배치)까지**다.

---

## 개정 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 0.1 | 2026-08-04 | 최초 작성 — 콘텐츠 구성 우선 플로우 + 3게이트 정의 |
| 0.2 | 2026-08-04 | Stage 2b(화면 설계/와이어프레임) 추가 + 디자인 파이프라인·네이밍 핸드오프 규약 정의 |
| 0.3 | 2026-08-07 | **이 플로우를 작업 공정의 단일 권위로 승격**(`.claude/CLAUDE.md` 자동 주입). A~E 체계 폐기. 리소스 생성 도구를 **ChatGPT 단일**로 확정(ComfyUI 미사용) + 배치 생성·후처리 파이프라인 명시 |
