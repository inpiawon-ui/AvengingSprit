# 재납품 — 유령 수색 판 그림, **목업 구도 그대로**

지금 판 그림은 목업을 보고 새로 그린 것이라 **구도가 다르다.** 목업은 골목 오른쪽에
`A-2` 벽 글씨와 `EXIT` 문이 있고, 가운데 아래에 금화가 쏟아진 열린 상자가 있는데,
지금 것은 그 자리가 비어 있고 짜임이 헐겁다.

## 기준 그림

```
Projects/AVSR/_exchange/ref/ghostsearch_ref.png   886 × 376 (1:1 크기)
```

기준 그림에는 게임이 따로 얹는 것들이 같이 찍혀 있다. **다음은 빼고 배경만 그린다.**

```
빼는 것 — 유령 · 말풍선(음표) · 「유령 수색」 · 「04:32:18」 · 「+ 12,640 G」 ·
          설명 글자 · 「보상 받기」 노란 버튼 · 빨간 느낌표 · 판 테두리
```

## 남기는 것 (기준 그림의 배경 구성 그대로)

- 왼쪽·가운데에 쌓인 나무 상자 더미
- 가운데 아래 **금화가 쏟아진 열린 보물상자** 와 바닥에 흩어진 금화
- 오른쪽 위 벽의 붉은 `A-2` 글씨
- 오른쪽 `EXIT` 초록 표지가 달린 문
- 오른쪽 상자 위에 앉은 **검은·흰 고양이**
- 철망 울타리, 뒤쪽 도시 야경과 가로등

## 내보낼 것

| 파일 | 크기 |
|---|---|
| `ghostsearchart.png` | 886 × 376 |

- 배경 그림이라 **네 귀까지 꽉 채운다**(투명 여백 없음)
- 판 테두리는 그리지 마라 — 게임이 따로 얹는다

## 지켜야 할 것

1. **글자를 그리지 마라.** 단 `A-2` 와 `EXIT` 는 벽·표지의 일부이므로 그린다
2. 마젠타를 쓰지 마라
3. 크기는 886 × 376 그대로

## 검수

기준 그림과 나란히 놓았을 때 **무엇이 어디에 있는지가 같아야** 한다 —
상자 더미 · 금화 상자 · `A-2` · `EXIT` · 고양이의 자리.

## 내보내는 곳

`Projects/AVSR/_exchange/in/ghostsearchart.png`

도형 스크립트로 만들지 마라. API 키를 쓰는 CLI 폴백은 쓰지 마라.
