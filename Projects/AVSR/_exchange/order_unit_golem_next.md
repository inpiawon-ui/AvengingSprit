# 발주 — 골렘 **오른쪽 아래 대각선(se)** 8자세 한 장 (768 × 96)

정면(s) 한 장이 통과했다. **같은 골렘**을 방향만 바꿔 그린다.

**반드시 먼저 열어 볼 것**: `Projects/AVSR/_exchange/in/_unit_golem_s_sheet.png` (통과한 정면 시트)
이 그림의 몸집 · 색 · 균열 위치 · 철근 위치를 그대로 따른다. **다른 골렘이 되면 안 된다.**

## 이번 방향

**오른쪽 아래를 향해 비스듬히 선 모습(se)** 이다. 쿼터뷰라 화면에서 오른쪽 앞으로 걸어오는 각이다.
- 얼굴과 가슴이 오른쪽 아래를 향한다. 왼쪽 어깨가 조금 뒤로, 오른쪽 어깨가 앞으로 나온다
- 정면보다 몸통이 약간 좁아 보이고, 옆구리가 한 쪽 보인다

## 한 장에 8자세 — 정면과 같은 순서

| 칸 | 자세 |
|---|---|
| 1 | 서 있음 | 
| 2 | 걷기 1 (왼발 앞) |
| 3 | 걷기 2 (오른발 앞) |
| 4 | 공격 1 (주먹 당김) |
| 5 | 공격 2 (주먹 내지름) |
| 6 | 맞음 (뒤로 젖혀지고 균열 빛 번쩍) |
| 7 | 쓰러짐 1 (무릎 꺾임) |
| 8 | 쓰러짐 2 (돌무더기) |

## 칸 규격 — 정면과 동일

| 항목 | 값 |
|---|---|
| 파일 | `unit_golem_se_sheet.png` (768 × 96) |
| 칸 | 96 × 96 여덟 칸, 왼쪽부터 순서대로 |
| 그림 세로 | 위에서 12 px ~ 아래에서 8 px |
| 발바닥 | 칸 아래에서 8 px |
| 좌우 | 발 중심이 칸 가로 48 px |
| 폭 | 어깨 기준 약 54 ~ 62 px |
| 배경 | **마젠타 `#FF00FF` 단색** |

색은 정면과 같다 — 밝은 면 `#8E8E84` · 바탕 `#6B6B63` · 그늘 `#45453F` · 외곽선 `#24241F` ·
철근 `#8A4B22` · 영혼빛 `#7CE8A0`.

## 검수 기준

1. 768 × 96, 배경 마젠타 한 색
2. 여덟 칸이 **정면 시트와 같은 골렘**(몸집 · 색 · 균열 · 철근이 이어진다)
3. 각 칸 발바닥 아래에서 8 px, 발 중심 가로 48 px
4. 방향이 오른쪽 아래 대각선으로 보인다

## 내보내는 곳

`Projects/AVSR/_exchange/in/unit_golem_se_sheet.png`
잠겨 있으면 `Projects/AVSR/_exchange/in/_generated/` 에 저장하고 그렇게 알려라.
작업용 스크립트·임시 파일은 프로젝트 밖 임시 폴더에서 쓰고 지운다.

API 키를 쓰는 CLI 폴백은 쓰지 마라.
