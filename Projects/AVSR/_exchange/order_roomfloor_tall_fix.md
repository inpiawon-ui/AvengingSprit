# 재발주 (4차) — `roomfloor_env_holding.png` 이음매 두 곳만 고친다 (720 × 1152)

## 3차는 거의 통과했다

3차 납품(`Projects/AVSR/_exchange/in/roomfloor_env_holding.png`)의 **가운데는 합격**이다.
원이 하나, 크기 그대로, 타일 · 좌우 벽이 이어지고, 새 줄 812/812 · 반복 0 도 맞다.
**그 가운데는 건드리지 마라.** 고칠 곳은 위 · 아래 이음매 둘뿐이다.

## 무엇이 틀렸나 (2배로 확대해 봤다)

| 자리 | 문제 |
|---|---|
| **위 이음매** — 줄 **170 ~ 205** | 좌우 타일(가로 약 x 30~185, x 535~690) 윗가장자리가 **세로 줄무늬로 번진 띠**가 됐다. 원본 같은 자리는 깨끗하다 |
| **아래 이음매** — 줄 **960 ~ 992** | 아래 담장 기둥 머리(주황 쇠붙이, 약 x 230~255 · x 465~490)가 **반투명 유령**처럼 한 번 더 비친다. 좌우 타일도 세로로 번졌다 |

원인은 이음매를 **원본과 겹쳐 흐리게 섞은 것**(크로스페이드 · 알파 섞기)이다.

## 어떻게 고치나

1. 3차 판을 바탕으로 한다
2. 줄 **260 ~ 900** 은 3차 **그대로** 둔다 (픽셀까지 같게)
3. 맨 위 180 줄 · 맨 아래 160 줄은 원본(`Projects/AVSR/_exchange/out/roomfloor_936/roomfloor_env_holding.png`) **그대로**
4. 줄 **180 ~ 260** 과 **900 ~ 992** 만 다시 그린다 — 그림 생성 · 편집으로
   - 원본의 마지막 줄(위: 179 · 아래: 992 에서 시작하는 담장)에서 **타일이 줄눈에서 딱 끊겨 새 타일이 시작**되게 그린다
   - **섞기 · 흐리기 · 반투명 겹치기 금지.** 원본을 비쳐 보이게 깔지 마라 — 유령이 거기서 생긴다
   - 원본 줄을 복사 · 반복 · 늘리는 것도 여전히 금지

## 검수 기준 (내가 이렇게 잰다)

1. 720 × 1152, 완전 불투명 RGB
2. 위 180 · 아래 160 줄이 원본과 픽셀까지 같다
3. 줄 260 ~ 900 이 3차와 픽셀까지 같다
4. 가운데 812 줄 중 원본에 없는 새 줄이 절반 이상, 같은 줄 반복 0
5. 이음 줄(180 · 992) 색 차이가 평소 줄 차이의 1.5배 이하
6. **번짐 없음** — 줄 160 ~ 1000 을 16 줄 창으로 **4 줄씩 밀며** (세로 색 변화 ÷ 가로 색 변화)를 재면
   **모든 창이 0.50 이상** (원본 바닥을 같은 식으로 재면 가장 낮은 창이 0.50 이다. 3차는 줄 168 · 172 에서 시작하는 창이 0.49 · 0.40 이었다)
7. 2배로 확대해 봐서 **기둥 유령 · 줄무늬 없음**

## 내보내는 곳

`Projects/AVSR/_exchange/in/roomfloor_env_holding.png` 로 덮어쓴다. 잠겨 있으면 `Projects/AVSR/_exchange/in/_generated/` 에.
이 한 장만. 작업 스크립트 · 임시 파일은 프로젝트 밖에서 쓰고 지운다.

API 키를 쓰는 CLI 폴백은 쓰지 마라.
