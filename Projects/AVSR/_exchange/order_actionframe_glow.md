# 재납품 — 하단 바 칸 액자 2장, **바깥 빛무리를 반투명으로**

방금 받은 두 장은 모양은 맞는데 **반투명 픽셀이 0개**다. 배경을 투명으로 정리하면서
테 바깥으로 번지던 빛이 통째로 깎였다. 목업은 테 둘레로 빛이 부드럽게 번진다.

## 기준 그림

```
Projects/AVSR/_exchange/ref/hostframe_ref.png    267 × 128
Projects/AVSR/_exchange/ref/playframe_ref.png    302 × 140
```

## 할 일

같은 두 장을 **빛무리만 살려서** 다시 내보낸다.

| 파일 | 크기 | 빛무리 |
|---|---|---|
| `actionframe_blue.png` | 267 × 128 | 테 둘레로 하늘색이 8~14 px 부드럽게 번진다 |
| `actionframe_gold.png` | 302 × 140 | 테 둘레로 금빛이 10~18 px 부드럽게 번진다 |

- 모양·색·안쪽 채움은 지금 것과 **똑같이**. 새로 디자인하지 마라
- 가장자리로 갈수록 알파가 **서서히** 0 이 된다

## 지켜야 할 것

1. 배경(빛무리 바깥)은 완전 투명(알파 0). 마젠타를 쓰지 마라
2. **배경 정리를 「알파 0 아니면 255」로 하지 마라** — 이번 발주가 깎인 이유다
3. 글자를 그리지 마라

## 검수

장당 **반투명 픽셀(알파 1~249)이 3,000개 이상**이어야 한다. 지금은 0개다.

## 내보내는 곳

`Projects/AVSR/_exchange/in/actionframe_blue.png` · `actionframe_gold.png`

도형 스크립트로 만들지 마라. API 키를 쓰는 CLI 폴백은 쓰지 마라.
