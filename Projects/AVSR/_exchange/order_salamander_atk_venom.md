# 발주 — 샐러맨더 공격 자세의 불꽃을 독색으로 (5장)

샐러맨더가 **독 캐릭터**로 바뀌었다(기획 2026-09-14). 투사체는 이미 청록 독불(`shot_venom`)로 바꿨는데,
**공격 자세 그림에 그려진 입 불꽃이 아직 주황**이라 입만 주황으로 남는다. 그 불꽃만 독색으로 바꾼다.

## 만들 것 — 5장

| 파일 (원본 = 결과 이름 같음) |
|---|
| `unit_salamander_s_atk1.png` |
| `unit_salamander_se_atk1.png` |
| `unit_salamander_e_atk1.png` |
| `unit_salamander_ne_atk1.png` |
| `unit_salamander_n_atk1.png` |

원본 위치: `Assets/BaseResource/Unit/salamander/` (각 96 × 96, 투명 배경 PNG)

## 무엇을 바꾸나 — **딱 두 색만 바꾼다**

불꽃은 정확히 두 색으로만 그려져 있다. 그 두 색을 아래로 바꾼다.

| 지금 색 | 바뀔 색 |
|---|---|
| `#F7AD00` (밝은 주황) | **`#7CE8A0`** (연둣빛 초록) |
| `#D67300` (주황) | **`#2FA86A`** (청록 초록) |

- ⚠ **그 두 색 말고는 한 픽셀도 건드리지 마라.** 도마뱀 몸·옷·눈·그림자·투명 영역 전부 그대로다
- ⚠ 새로 그리지 마라. **색 치환**이다. 모양·크기·위치가 달라지면 반려다
- 알파(투명)는 원본 그대로 둔다

## 검수 기준 (내가 이렇게 잰다)

1. 96 × 96, 투명 PNG
2. 원본에서 `#F7AD00` 이던 픽셀은 전부 `#7CE8A0`, `#D67300` 이던 픽셀은 전부 `#2FA86A`
3. **나머지 픽셀은 원본과 완전히 같다**(색·알파 모두). 다른 픽셀이 하나라도 바뀌면 반려
4. 다섯 장 모두 같은 규칙으로 바뀐다

## 내보내는 곳

`Projects/AVSR/_exchange/in/` 에 **원본과 같은 파일명**으로 저장한다.
잠겨 있으면 `Projects/AVSR/_exchange/in/_generated/` 에 저장하고 그렇게 알려라.
작업용 스크립트·임시 파일은 프로젝트 밖 임시 폴더에서 쓰고 지운다.

API 키를 쓰는 CLI 폴백은 쓰지 마라.
