# 발주 — 샐러맨더 독불 투사체 3프레임 (24 × 24)

샐러맨더가 **독 캐릭터**로 바뀌었다(기획 2026-09-14). 지금은 주황 불덩이를 쏘는데,
**독을 머금은 청록 불**로 바꾼다. 불은 불인데 초록빛이 도는, 라이터 불꽃 같은 느낌이다.

## 만들 것

| 파일 | 내용 |
|---|---|
| `shot_venom_1.png` | 갓 태어난 작은 불꽃 |
| `shot_venom_2.png` | 활짝 핀 불꽃 |
| `shot_venom_3.png` | 꼬리가 늘어진 불꽃 |

- 크기 **24 × 24 px** 고정, 세 장 모두 같은 크기
- 세 장이 이어지면 **타오르는 것처럼** 보여야 한다(같은 불꽃의 세 순간)
- 진행 방향은 **오른쪽**. 꼬리는 왼쪽으로 흐른다
- 그림은 캔버스 가운데에 둔다

## 색 — 지금 불덩이의 색을 독으로 바꾼 것

지금 `shot_flame` 이 쓰는 색(참고):
`#941000` 어두운 빨강 · `#D67300` 주황 · `#F7AD00` 밝은 주황 · `#F7F773` 노란 심지

독불은 이렇게 간다.
- 바깥 어두운 테두리 `#1B4A2E` (짙은 초록)
- 몸통 `#2FA86A` (청록 초록)
- 밝은 부분 `#7CE8A0` (연둣빛 초록)
- 심지 `#E8FFC8` (거의 흰 연두)

밝기 단계는 지금 불덩이와 **같게** 간다 — 색만 바뀌고 대비는 그대로다.

## 규격

| 항목 | 값 |
|---|---|
| 배경 | **마젠타 `#FF00FF` 단색**(투명은 내가 뚫는다). 그림 안쪽에 마젠타를 쓰지 마라 |
| 형식 | PNG · 24 × 24 |
| 화풍 | 지금 게임의 픽셀아트. 안티에일리어싱 없이 또렷하게 |

## 내보내는 곳

`Projects/AVSR/_exchange/in/` 에 위 파일명 그대로 저장한다.
잠겨 있으면 `Projects/AVSR/_exchange/in/_generated/` 에 저장하고 그렇게 알려라.
작업용 스크립트·임시 파일은 프로젝트 밖 임시 폴더에서 쓰고 지운다.

API 키를 쓰는 CLI 폴백은 쓰지 마라.
