# 발주 — 방 바닥 한 장만: `roomfloor_env_lab.png` (720 × 1152) · 마지막 장

## 방식은 지난 일곱 장과 같다

구치소 · 거리 · 압착기 · 슬러지 · 가디언 · 킹핀 · 로봇뱀이 이 방식으로 통과했다.
**가운데를 그림 편집으로 새로 그린다.** 원본 줄을 복사해서 채우면 반려다.

## 이번에 만들 것 — 한 장뿐이다

| 항목 | 값 |
|---|---|
| 원본 | `Projects/AVSR/_exchange/out/roomfloor_936/roomfloor_env_lab.png` (720 × 936) |
| 결과 | `Projects/AVSR/_exchange/in/roomfloor_env_lab.png` (720 × **1152**) |
| 통과한 예 | `Assets/BundleResource/RoomFloor/roomfloor_env_holding.png` |

연구소 바닥이다. 베이지빛 금속 판을 이어 붙인 바닥으로, 판과 판 사이에 이음 줄이 지나가고
군데군데 **작은 통풍구**(격자 무늬 네모)가 박혀 있다. 위아래 끝에는 배관 설비가 있다.

## 만드는 법

1. 맨 위 **180 px** = 원본 위 180 px 그대로 (붙여 넣어도 된다)
2. 맨 아래 **160 px** = 원본 아래 160 px 그대로
3. 그 사이 **812 px** 를 **그림 생성·편집으로 새로 그린다**
   - 금속 판 크기와 이음 줄 간격은 원본과 같게. 판 줄이 위아래로 어긋나지 않게 이어진다
   - **통풍구는 잘리지 않게** 온전한 것만 둔다. 개수는 늘어난 높이에 맞게 자연스럽게
   - 좌우 벽은 위에서 아래까지 끊김 없이 이어진다
4. 위 180 · 아래 160 과 만나는 두 이음 줄 둘레만 다듬는다
   - ⚠ **원본과 흐리게 섞지 마라(크로스페이드·알파).** 줄무늬와 유령이 생긴다

## 검수 기준 (숫자로 잰다)

1. 720 × 1152, 완전 불투명 RGB
2. 위 180 px · 아래 160 px 가 원본과 픽셀까지 같다
3. **가운데 812줄 중 원본에 없는 새 줄이 절반(406줄) 이상**
4. **가운데에 같은 줄이 두 번 나오지 않는다**
5. 이음 줄(180 · 992)의 색 차이가 평소 줄 차이의 2배 이하
6. 줄 160~1000 을 16줄 창으로 4줄씩 밀며 (세로 변화 ÷ 가로 변화) 를 재면 모든 창이 원본 최저값 이상
7. 2배로 확대해 봐서 잘린 통풍구가 없다

만들고 나서 **3번과 4번을 스스로 세어 보고** 통과한 것만 저장해라.

## 내보내는 곳

`Projects/AVSR/_exchange/in/roomfloor_env_lab.png` 로 덮어쓴다.
그 파일이 잠겨 있으면 `Projects/AVSR/_exchange/in/_generated/roomfloor_env_lab.png` 에 저장하고 그렇게 알려라.
작업용 스크립트·임시 파일은 프로젝트 밖 임시 폴더에서 쓰고 지운다.

API 키를 쓰는 CLI 폴백은 쓰지 마라.
